import SwiftUI
import Combine

struct ContentView: View {

    
    var body: some View {
        VStack {
            Text("Calculator")
                .font(.largeTitle)
                .padding()
        }
    }

}