import SwiftUI
import Combine

struct ContentView: View {

    
    var body: some View {
        VStack {
            Text("Notes")
                .font(.largeTitle)
                .padding()
        }
    }

}