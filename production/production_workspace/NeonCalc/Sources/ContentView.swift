import SwiftUI
import Combine

struct ContentView: View {

    
    var body: some View {
        VStack {
            Text("NeonCalc")
                .font(.largeTitle)
                .padding()
        }
    }

}