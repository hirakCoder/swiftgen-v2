import SwiftUI
import Combine

struct ContentView: View {

    
    var body: some View {
        VStack {
            Text("TodoList")
                .font(.largeTitle)
                .padding()
        }
    }

}